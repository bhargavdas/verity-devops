pipeline {
    agent any
    stages {
      stage('Clean') {
        steps {
          sh 'mvn clean'
        }
      }
      //Code starts for stage PMD
		stage('PMD') {
            steps {
                sh 'mvn site'
            }
        }
		//Code ends for stage PMD
	  }
}